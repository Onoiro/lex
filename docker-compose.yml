services:
  lex-proxy:
    build:
      context: .
      dockerfile: proxy/Dockerfile
    ports:
      - "8004:8004"
    env_file:
      - ./.env
    volumes:
      - lex-cache:/app/data
    restart: unless-stopped

volumes:
  lex-cache: